# NeoVim custome configs

1. Install the NeoVim https://neovim.io/doc/install/
3. Install ripgrep for searching word in files
2. Close the configurations `git clone https://github.com/hicugi/neovim-config.git ~/.config/nvim`
